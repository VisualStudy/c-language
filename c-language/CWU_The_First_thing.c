#include <stdio.h> // 모니터 화면, 마우스, 키보드 등 기본 입출력 장치 위해
#include <stdlib.h> // 스탠다드 라이브러리에 담긴 기능을 위하여
#include <memory.h> // 동적 할당
#include <time.h> // 시간 다루기
#include <windows.h> // 윈도우 운영체제 제어(다른 운영체제에선 X)
#pragma warning (disable:4996) // 기본적으로 c++를 c처럼 쓰기 위해서
//하지만 굳이 c++로 c를 돌릴 필요는 없지 않을까?

void main()
{

}