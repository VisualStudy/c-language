#include <stdio.h>

int main(void)
{
    int ch;
    char str[100] = "rabarataRaVaraTA";
    
    while ((ch = getchar()) != EOF)
    {
        putchar(ch);
    }

    while (str != '\0')
    {

    }
    
    return 0;
}